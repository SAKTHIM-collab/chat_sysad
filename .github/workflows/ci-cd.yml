name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.server
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/chat-server:latest
            ${{ secrets.DOCKER_USERNAME }}/chat-server:${{ github.sha }}

      - name: Deploy to Server (Placeholder - for actual deployment, use SSH or similar)
        run: |
          echo "Simulating deployment to a server..."
          echo "This step would typically involve SSH'ing into your server and running scripts/deploy.sh"
          echo "For example: ssh user@your_server_ip 'cd /path/to/chat_system && ./scripts/deploy.sh'"
          # Example using SSH:
          # - uses: appleboy/ssh-action@master
          #   with:
          #     host: ${{ secrets.SSH_HOST }}
          #     username: ${{ secrets.SSH_USERNAME }}
          #     key: ${{ secrets.SSH_PRIVATE_KEY }}
          #     script: |
          #       cd /path/to/your/chat_system_repo
          #       git pull origin main
          #       ./scripts/deploy.sh
